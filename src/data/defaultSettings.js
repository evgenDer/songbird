const MAX_SCORE = 30;
const MAX_SCORE_ROUND = 5;

const DEFAULT_SETTINGS = {
  currentRound: 0,
  isRightAnswer: false,
  rightAnswerNumber: 1,
  isEndGame: false,
  currentScore: 0,
};

export { DEFAULT_SETTINGS, MAX_SCORE, MAX_SCORE_ROUND };
